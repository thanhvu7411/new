## Cursor Cloud specific instructions

This is currently an empty repository (initialized with only a `README.md`). There are no services, dependencies, or build systems to configure.

When application code is added to this repository, update this section with:
- How to install dependencies and which package manager to use
- How to run, build, lint, and test the application
- Any non-obvious startup caveats or environment requirements
